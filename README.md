# Tinder.Api

### Install:
- Step 1: ```npm install -g yarn```
- Step 2: ```yarn```
### Config:
- change content in ```config``` to require config in this
### Run debug or start: 
```yarn dev``` or ```yarn start```
